View Branches
$ git branch

Create new branch and view branches and switch to new branch
$ git branch dict-slice
$ git branch
$ git checkout dict-slice
$ git branch

Shortcut to above
$ git checkout -b dict-slice
